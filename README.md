# SkillSetGo

For frontend setup 
npm i install 
npm run dev

For backend setup 
cd backend
touch .env 
Open .env file and put MONGO_PASSWORD= enterthepassword
//access needed in mongo 
npm i package.json  
node server.js
